import factory from '@adonisjs/lucid/factories'
import CandidateActivity from '#models/candidate_activity'

export const CandidateActivityFactory = factory
  .define(CandidateActivity, async ({ faker }) => {
    return {}
  })
  .build()
